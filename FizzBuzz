#include <iostream>

#define LOG(x) std::cout << x;

int main()
{
	for (int i = 1; i < 101; i++)
	{
		if (i % 3 == 0 && i % 5 == 0)
		{
			LOG("FizzBuzz\n");
		}
		else if (i % 3 == 0)
		{
			LOG("Fizz\n");
		}
		else if (i % 5 == 0)
		{
			LOG("Buzz\n");
		}
		else 
		{
			LOG(i);
			LOG("\n");
		}

	}
}
